Implement a complete "Lesson Plan Generator" feature inside my existing system. Follow these instructions precisely:

=== PART 1: DATABASE SCHEMA ===

Create the following tables (use whatever database/ORM language my system already uses - show the SQL/ORM models accordingly):

1. `schools` (if not already present)
   - id, name, district, po_box, logo_url

2. `users`
   - id, school_id (FK), full_name, role (ENUM: 'teacher', 'academic_teacher', 'headmaster', 'admin'), email/username, password_hash

3. `teacher_subjects` (many-to-many: a teacher can teach multiple subjects/classes)
   - id, user_id (FK), subject_id (FK), form_id (FK)

4. `subjects`
   - id, name (e.g. Mathematics, English, B/Studies)

5. `forms`
   - id, name (Form One, Form Two, Form Three, Form Four)

6. `main_competences`
   - id, subject_id (FK), form_id (FK), code (e.g. "1.0"), title

7. `specific_competences`
   - id, main_competence_id (FK), code (e.g. "1.1"), title

8. `learning_activities`
   - id, specific_competence_id (FK), code (e.g. "a"), activity_description
   - suggested_methods (TEXT or JSON array)
   - assessment_criteria (TEXT)
   - resources (TEXT or JSON array)
   - periods_allocated (INTEGER, can be null if not yet set)

9. `generated_lesson_plans`
   - id, teacher_id (FK), subject_id (FK), form_id (FK), specific_competence_id (FK)
   - learning_activity_ids (JSON array - can be more than one)
   - date, number_of_periods, time_minutes
   - registered_boys, registered_girls, present_boys, present_girls
   - content (JSON - full AI output: main_activity, specific_activity, resources, references, stages[])
   - status (ENUM: 'draft', 'final')
   - created_at, updated_at

Set appropriate foreign key constraints and indexes (e.g. an index on subject_id + form_id in main_competences for fast lookups).

=== PART 2: TEACHER PAGE - "Generate Lesson Plan" ===

Build a new page/route (e.g. `/lesson-plans/create`) accessible only to teacher-role accounts, with these components:

1. Auto-fill: school name and teacher name pulled from the logged-in session (not manually re-typed)
2. "Subject" dropdown - show ONLY subjects assigned to that teacher (from the teacher_subjects table), not all subjects in the system
3. "Form/Class" dropdown - show ONLY classes assigned to that teacher
4. After subject+form are selected: "Main Competence" dropdown (queried from main_competences filtered by subject_id+form_id)
5. After main competence is selected: "Specific Competence" dropdown/checkbox list (from specific_competences)
6. After specific competence is selected: show the list of its "Learning Activities" (checkboxes - the teacher can select one or more matching the number of periods)
7. Number fields: "Number of periods", "Time per period (minutes)"
8. Date picker and student counts (registered/present - boys/girls)
9. "Generate Lesson Plan" button - must validate that all required fields are filled before submitting
10. After the AI responds: display the result in an editable form with status "Draft", with "Edit", "Save", and "Download as Word (.docx)" buttons
11. The Word export MUST follow the EXACT structure of the template I gave you earlier (the TIE Lesson Plan template) - not any other layout

=== PART 3: AI INTEGRATION (backend service) ===

Build an endpoint/service (e.g. `POST /api/lesson-plans/generate`) that does the following:

1. Receive: subject_id, form_id, specific_competence_id, selected learning_activity_ids, number_of_periods, time_minutes
2. Fetch from the database: suggested_methods, assessment_criteria, resources for the selected learning_activities (THIS DATA ALREADY EXISTS FROM THE SYLLABUS - do not ask the AI to invent it)
3. Send the AI (Claude/GPT API) a prompt structured like this - the AI's job is to ORGANIZE and MERGE real data, NOT invent new content:

   "You have this real data from the TIE Curriculum:
   - Source activity: [suggested_methods from DB]
   - Assessment Criteria: [from DB]
   - Resources: [from DB]
   - Number of periods: [N], Time: [M minutes per period]
   
   Organize this data into JSON with this structure:
   {
     'main_activity': '...',
     'specific_activity': '...',
     'teaching_learning_resources': '...',
     'references': '...',
     'stages': [
       {'stage':'Introduction','time_minutes':'...','teaching_activities':'...','learning_activities':'...','assessment_criteria':'...'},
       {'stage':'Competence Development', ...},
       {'stage':'Design', ...},
       {'stage':'Realisation', ...}
     ]
   }
   
   Important: USE the wording and information provided above - do not invent teaching methods that are not present in the data given to you. Distribute time according to the number of periods and minutes provided."

4. Save the response into generated_lesson_plans with status='draft'
5. Return it to the frontend for display/editing

=== PART 4: HEADMASTER/ADMIN PAGE - "Syllabus Manager" (SEED DATA) ===

Build a new page/route (e.g. `/admin/syllabus-manager`) accessible ONLY to role = 'headmaster' or 'admin', with TWO ways to enter syllabus data:

**METHOD A - Manual entry form:**
- Form to add a Main Competence (select subject+form, enter code and title)
- Form to add a Specific Competence under a given Main Competence
- Form to add a Learning Activity under a Specific Competence (activity description, suggested methods - textarea accepting a list, assessment criteria, resources, number of periods)
- Ability to edit and delete each entry
- A treeview/accordion display: Subject → Form → Main Competence → Specific Competence → Learning Activities, so the admin can see the whole structure clearly

**METHOD B - Bulk import via CSV/JSON:**
- An "Upload file" button that accepts CSV or JSON with columns: subject, form, main_competence_code, main_competence_title, specific_competence_code, specific_competence_title, activity_code, activity_description, suggested_methods, assessment_criteria, resources, periods_allocated
- After upload: show a "preview" of the data to be imported (how many rows, any errors - e.g. rows with missing fields) BEFORE confirming
- A "Confirm import" button - only then does the data actually get written to the database
- Show a results report after import: e.g. "Added 3 Main Competences, 9 Specific Competences, 45 Learning Activities. 2 errors (row 12, row 30 - reason)."

Also provide a downloadable template/example of that CSV (an empty file with just the column headers) that the admin can fill in and re-upload.

=== PART 5: PERMISSIONS ===

- Teacher: sees/uses ONLY the "Generate Lesson Plan" page, and sees ONLY their own generated lesson plans
- Headmaster/Admin: sees the "Syllabus Manager" page for managing syllabus data, and (if needed) can view all teachers' lesson plans for monitoring purposes - not editing them
- Implement role-based access control (RBAC) middleware on every relevant route

=== ACCEPTANCE CRITERIA ===

- A teacher can select subject → form → competence → activity and generate a lesson plan without touching the database directly
- A headmaster can add Main/Specific Competences and Learning Activities for a new subject without developer assistance
- Bulk CSV/JSON import works and shows errors before confirming
- The Word export matches the TIE Lesson Plan template structure 100%
- Each teacher sees only the subjects/classes they are assigned to